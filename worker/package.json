{
  "name": "dogmatch-gemini-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker als Gemini-Vision-Proxy fuer DogMatch AI",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put GEMINI_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
